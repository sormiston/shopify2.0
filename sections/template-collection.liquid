{% paginate collection.products by 6 %}
  {% for product in collection.products %}
    {% if product.featured_image %}
        {{product.featured_image | image_url: 'medium'}}
      <img src="{{product.featured_image | image_url: width: 400 }}" alt="{{product.featured_image.alt}}">
    {% endif %}
    <span>{{ product.vendor }}</span>
    <a href="{{product.url}}">{{ product.title }}</a>
  {% else %}
    <p>There are no products inside of this collection.</p>
  {% endfor %}

  {% render 'paginate', pagination: paginate %}
{% endpaginate %}
