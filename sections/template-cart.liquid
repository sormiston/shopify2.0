{% if cart.item_count > 0 %}
  <!-- CART HAS ITEMS -->
{% else %}
  <!-- CART IS EMPTY -->
  <div class="max-w-6xl h-60 my-4 flex items-center justify-center">
    <div class="text-center">
      <div class="text-3xl my-4 mx-auto">Your cart is empty</div>
      <div class="my-4 py-4">
        <a
          href="
            {{routes.all_products_collection_url
            }}
          "
          class="border border-gray-600 text-white bg-gray-600 px-8 py-3"
          >Continue shopping</a
        >
      </div>
    </div>
  </div>
{% endif %}
