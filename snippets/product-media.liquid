{% case media.media_type %}
  {% when 'image' %}
    <img
      src="{{ media | image_url: width: 1200 }}" alt="{{ media.preview_image.alt }}" class="w-full h-full object-cover">
  {% else %}

{% endcase %}
